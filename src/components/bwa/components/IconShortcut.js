import React from 'react';

const IconShortcut = (props) => {
    return (
        <div className={props.className}>
                <img src={props.src} />
        </div>
    )
}

export default IconShortcut;